//! `RepoReconcileManager` MF-* test suite. Populated incrementally
//! alongside the implementation; this file exists so the outer
//! `#[cfg(test)] mod tests;` declaration compiles before the
//! individual test bodies land.
